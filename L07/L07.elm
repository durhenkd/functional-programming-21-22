
{- E 7.1.1  Add a button with the text Create account that is disabled if the password or username
            fields are empty. You don’t have to add the onClick attribute to the button.
            
            IMPLEMENTED IN Inputs.elm-}

{- E 7.1.2  Add a new input field where the user has to repeat their password, to ensure that the
            password matches. Display a green success message if the passwords match or red error
            message if the passwords are different. The submit button should also be disabled if the
            passwords don’t match.
            
            IMPLEMENTED IN Inputs.elm-}


{- E 7.1.3  If the account is for administrators (has type Admin ), the password must have at least
            12 characters. For regular users it must have at least 8 characters.
            Display an error message and disable the submit button if the password doesn’t meet the
            specified criteria.

            IMPLEMENTED IN Inputs.elm-}


{- E 7.6.1  Run the tails function to check if it behaves according to the examples. If not, find
            and fix the bug.
            
            IMPLEMENTED IN Documentation.elm-}


{- E 7.6.2  Implement the combinations function such that the tests pass.
            
            NOT IMPLEMENTED IN Documentation.elm-}

{- E 7.6.3  Display the population and population density (population/area) for each country.
            
            IMPLEMENTED IN Countries.elm-}

{- E 7.6.4  Sort the countries by area in descending order.
            
            IMPLEMENTED IN Countries.elm-}

{- E 7.6.5  Add a checkbox which allows the user to change the current sort order of countries to
            ascending (i.e. if the checkbox is is unchecked the sort order will be descending, if it
            checked the sort order will be ascending)
            
            IMPLEMENTED IN Countries.elm -}

{- E 7.6.6  Add a dropdown list (select element) to select the field for the sorting. The options
            should be: population, area, population density.
            
            IMPLEMENTED IN Countries.elm-}

{- E 7.6.7* Add a text field which will allow to filter countries by name, using the String.contains
            function.
            
            IMPLEMENTED IN -}

{- E 7.6.8* Add a set of checkboxes to allow filtering the currently shown countries by region.
            
            IMPLEMENTED IN -}